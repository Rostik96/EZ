spring:
  liquibase:
    enabled: true
  datasource:
    url: jdbc:h2:mem:dev
    username:
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: validate
  h2:
    console:
      enabled: true
